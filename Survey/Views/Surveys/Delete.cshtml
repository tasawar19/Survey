@model Survey.Models.Survey

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Survey</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SurveyTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SurveyTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.EndDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EndDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SurveyUrl)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SurveyUrl)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TimeLimit)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimeLimit)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NoOfView)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NoOfView)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NoOfResponses)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NoOfResponses)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @if (Model.NoOfResponses.HasValue && (!Model.Status.HasValue || Model.Status.Value))
            {
                <input type="submit" value="Disable" class="btn btn-default" /> 
            }
            else if (Model.NoOfResponses.HasValue && !Model.Status.Value)
            {
                <input type="submit" value="Enable" class="btn btn-default" /> 
            }
            else
            {
                <input type="submit" value="Delete" class="btn btn-default" /> 
            }
            @*<input type="submit" value="Delete" class="btn btn-default" />*@ |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
